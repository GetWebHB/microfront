{
  "name": "module-federation",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:app1": "lerna run start --scope app1",
    "build:app1": "lerna run build --scope app1",
    "start:app2": "lerna run start --scope app2",
    "build:app2": "lerna run build --scope app2",
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
